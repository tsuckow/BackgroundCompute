<project name="Hudson Background Pi" default="deploy" basedir=".">
	<target name="printinfo">
		<property environment="env" />
		<echo message="${env.BUILD_TAG}"/>
	</target>
	<target name="init" depends="printinfo">
		<property name="sourceDir" value="src" />
		<property name="outputDir" value="classes" />
		<property name="requiredDir" value="Required" />
		<property name="deployDir" value="/home/webmaster/public_html/updates/bc/dev" />
	</target>
	<target name="clean" depends="init">
		<delete dir="${outputDir}" />
	</target>
	<target name="prepare" depends="clean">
		<mkdir dir="${outputDir}" />
	</target>
	<target name="compile" depends="prepare">
		<javac srcdir="${sourceDir}" destdir="${outputDir}" />
	</target>
	<target name="deploy" depends="compile">
		<delete dir="${deployDir}" />
		<mkdir dir="${deployDir}" />
		<copydir src="${outputDir}" dest="${deployDir}" />
		<copydir src="${requiredDir}" dest="${deployDir}" />
	</target>
</project>
